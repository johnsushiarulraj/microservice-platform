# Container registry configuration
# All services pull from this registry
REGISTRY=ghcr.io/johnsushiarulraj

# Infrastructure service images (pulled by start-infra.sh)
GATEWAY_IMAGE=${REGISTRY}/api-gateway:latest
DEVCONSOLE_IMAGE=${REGISTRY}/devconsole:latest
