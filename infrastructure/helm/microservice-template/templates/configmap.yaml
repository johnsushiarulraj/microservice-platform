apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "microservice-template.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
data:
  SPRING_PROFILES_ACTIVE: {{ .Values.env.SPRING_PROFILES_ACTIVE | quote }}
  SERVER_SERVLET_CONTEXT_PATH: "/template"
